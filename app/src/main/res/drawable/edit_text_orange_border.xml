<?xml version="1.0" encoding="utf-8"?>
<!-- edit_text_red_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/white" /> <!-- Transparent color -->
    <stroke
        android:width="2dp"
        android:color="@android:color/holo_orange_dark" /> <!-- Border color (red) -->
    <corners android:radius="8dp" /> <!-- Adjust the radius to your preference for rounded corners -->
</shape>
